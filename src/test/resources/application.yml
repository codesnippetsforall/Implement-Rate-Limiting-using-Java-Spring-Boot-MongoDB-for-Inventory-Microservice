# Embedded MongoDB (Flapdoodle): use a version with Windows builds (see Flapdoodle docs).
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.15

spring:
  data:
    mongodb:
      database: inventory-test
